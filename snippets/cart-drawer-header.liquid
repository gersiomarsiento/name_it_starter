<div class="shadow-sm">
  <div class="w-full flex flex-row items-center gap-5 justify-between px-5 py-4">
    {%- if section.settings.heading != blank -%}
      <h2 class="h4">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <button
      class="js-cart-drawer-close"
      type="button"
      aria-label="{{ 'accessibility.close' | t }}"
    >
      {%- render 'icon-close' -%}
    </button>
  </div>
</div>
